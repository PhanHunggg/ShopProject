function Product(name, price, screen, blackCamera, fontCamera, image, desc, type) {
  this.name = name;
  this.price = price;
  this.screen = screen;
  this.blackCamera = blackCamera;
  this.fontCamera = fontCamera;
  this.image = image;
  this.desc = desc;
  this.type = type;
}
